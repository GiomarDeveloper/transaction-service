package com.bank.transaction.config;

/**
 * Clase de configuración para MongoDB.
 * Puede contener configuraciones personalizadas para la conexión a MongoDB.
 * Actualmente vacía, lista para extensiones futuras.
 */
public class MongoConfig {
  // Esta clase está preparada para futuras configuraciones personalizadas de MongoDB
  // como configuraciones de conexión, índices, o políticas de consulta
}
